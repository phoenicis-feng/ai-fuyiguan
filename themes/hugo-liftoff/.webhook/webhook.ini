[program:webhook]
directory=%(ENV_HOME)s/www-head/webhook
environment=DEPLOY_SECRET_LIFTOFF=
command=/home/head/www-head/webhook/webhook -hooks hooks.json.tmpl -template -logfile %(ENV_HOME)s/logs/webhook.log -hotreload -verbose
startsecs=15
